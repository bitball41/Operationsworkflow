{
  "name": "operations-workflow",
  "private": true,
  "version": "2.0.0",
  "description": "Plain JavaScript operating system for a website-selling business.",
  "scripts": {
    "test": "node tests/openscout-engine.test.cjs && node --test tests/render-smoke.test.mjs",
    "check": "npm test"
  }
}
